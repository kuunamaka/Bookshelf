name: GitHub Actions
run-name: formatter and jest are running 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: install npm
        uses: npm install
      - name: run prettier
        uses: npm run format
      - name: run eslint
        uses: npm run lint
      - name: run jest
        uses: npm run test
